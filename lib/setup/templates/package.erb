require 'setup'

class <%= package_class_name %> < Setup::Tasks::Package
  package_name '<%= package_name %>'

  def steps
<% for file in files %>
    yield file '<%= file %>'
<% end %>
<% for package in packages %>
    yield package '<%= package %>'
<% end %>
  end
end
